module RefineExpressions where

open import Data.Nat using (ℕ; zero; suc; _+_)
open import Data.Unit using (⊤; tt)

solving : ℕ
solving = ?

twoHoles : ℕ
twoHoles = ?

unrefinable : ℕ
unrefinable = ?
