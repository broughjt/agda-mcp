module ElaborateNormalization where

open import Data.Nat using (ℕ; _+_)

twice : ℕ → ℕ
twice n = n + n

asIs : ℕ
asIs = ?

instantiated : ℕ
instantiated = ?

headNormal : ℕ
headNormal = ?

simplified : ℕ
simplified = ?

normalised : ℕ
normalised = ?
