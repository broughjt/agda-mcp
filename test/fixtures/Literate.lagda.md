# A literate Agda fixture

The source edit must leave this prose and the code fence untouched.

```agda
module Literate where

identity : (A : Set) → A → A
identity A x = {!!}
```

This prose follows the goal.
