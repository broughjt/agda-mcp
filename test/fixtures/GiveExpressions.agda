module GiveExpressions where

open import Data.Nat using (ℕ; zero; suc; _+_)

illTyped : ℕ
illTyped = ?

outOfScope : ℕ
outOfScope = ?

unparseable : ℕ
unparseable = ?

verbatim : ℕ
verbatim = ?

parenthesized : ℕ
parenthesized = suc ?

redundantParentheses : ℕ
redundantParentheses = ?

underscore : ℕ
underscore = ?
