module GiveFamily where

open import Data.Nat using (ℕ; zero; suc; _+_)
open import Data.Unit using (⊤; tt)

given : ℕ
given = ?

refined : ℕ
refined = ?

introduced : ⊤
introduced = ?

elaborated : ℕ
elaborated = ?
